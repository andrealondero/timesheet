<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sys="clr-namespace:System;assembly=netstandard" x:Class="timesheet.Views.PageACompiler" Title="COMPILER">

    <StackLayout Margin="20" VerticalOptions="StartAndExpand">
        <Label Text="Date" />
        <DatePicker Format="yyyy.MM.dd" Date="{x:Binding Date}" MinimumDate="2001.01.01"/>
        <Label Text="Worked hours" />
        <Entry Text="{Binding Hours}" />
        <Label Text="Description" />
        <Editor Text="{Binding Description}" HeightRequest="80" MaxLength="150" />
        <Button Text="Save" Clicked="OnSaveClicked" />
        <Button Text="Delete" Clicked="OnDeleteClicked" />
        <Button Text="Cancel" Clicked="OnCancelClicked" />
        <Button Text="Speak" Clicked="OnSpeakClicked" />
    </StackLayout>

</ContentPage>